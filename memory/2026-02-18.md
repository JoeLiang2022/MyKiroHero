# 2026-02-18 Daily Log

## MCP Tool Consolidation — Phase 0 Dispatch

### What happened
- Attempted to dispatch Phase 0 (Role-Based Filtering + Alias infrastructure) to Workers via MC plan
- Created 3 plans total due to repeated failures:
  - `plan-20260218-191957-73f` (1st attempt) — task never picked up, workers stuck busy
  - `plan-20260218-192935-94d` (2nd attempt) — same issue
  - `plan-20260218-193140-b8f` (3rd attempt) — finally dispatched after reset_worker + drainQueue trigger

### Critical bugs discovered
4 issues opened in BugTracker:

1. **CRITICAL** `issue-20260218-194011-feb` — Heartbeat broadcasts to ALL Kiro windows (including Workers)
   - Worker-3 got stuck in infinite heartbeat loop, burning tokens
   - Root cause: `kiro-cli-handler.js` uses `kiroAgent.sendMainUserInput` which broadcasts to all windows
   - **FIX APPLIED**: Changed `index.js` onRegister callback to use `sendDirectReply` (WA only) instead of `triggerHeartbeat`
   - **Gateway restarted** to apply fix
   - Worker steering also updated with `[Heartbeat]` ignore rule
   - **REMAINING**: Other `triggerHeartbeat` calls in `server.js` (review pipeline notifications) still broadcast — lower frequency but same issue

2. **HIGH** `issue-20260218-194450-918` — MC task `assignedTo` always null
   - `_syncToMC()` in task-executor.js doesn't sync assignedTo field
   - Caused Commander to think tasks weren't dispatched → triple dispatch
   - **NOT FIXED YET**

3. **HIGH** `issue-20260218-194500-9aa` — reset_worker ineffective
   - Worker heartbeat immediately re-marks worker as busy after reset
   - Only workaround: close Worker Kiro window and reopen
   - **NOT FIXED YET**

4. **MEDIUM** `issue-20260218-194455-1ca` — setup-worker.js PRESERVE_FILES ReferenceError
   - `sync-worker-steering.js` → `setup-worker.js --update` fails
   - Workaround: manual `Copy-Item` to sync steering files
   - **NOT FIXED YET**

### Current state
- worker-1: Was running Phase 0 task `task-20260218-192942-d25` on branch `worker/task-20260218-192942-d25`
  - Gateway was restarted mid-task — task queue lost, but worker may still be working
  - When worker-1 calls `report_task_result`, it may fail (task not found in queue)
  - Code changes are on the branch — can be reviewed manually
- worker-2: Was running same Phase 0 (duplicate) — 大叔 closed it
- worker-3: Was stuck in heartbeat loop — 大叔 closed it, steering updated

### Files changed this session
- `MyKiroHero/src/gateway/index.js` — Changed onRegister to use sendDirectReply instead of triggerHeartbeat
- `MyKiroHero/templates/worker-steering/AGENTS.md` — Added `[Heartbeat]` ignore rule
- `Worker1/.kiro/steering/AGENTS.md` — Synced ignore rule
- `Worker2/.kiro/steering/AGENTS.md` — Synced ignore rule
- `Worker3/.kiro/steering/AGENTS.md` — Synced ignore rule
- `.kiro/steering/TOOLS.md` — Added Worker Dispatch lessons

### TODO for next session
1. Check if worker-1 completed Phase 0 — look at branch `worker/task-20260218-192942-d25` in Worker1 repo
2. If worker-1 failed (report_task_result couldn't find task after Gateway restart), review its code manually and merge
3. Fix remaining 3 bugs (assignedTo sync, reset_worker, setup-worker.js)
4. After Phase 0 is confirmed done, proceed to Phase 1 (4 parallel tasks: Issue, MC, Worker, WhatsApp merges)
5. Consider fixing ALL triggerHeartbeat calls in server.js to not broadcast to Workers

### Key lesson
- `triggerHeartbeat` → `sendToKiroChat` → Kiro REST API broadcasts to ALL open Kiro windows
- Never use triggerHeartbeat for notifications that Workers shouldn't see
- MC task assignedTo is unreliable — check actual worker registry via REST `/api/workers` instead
