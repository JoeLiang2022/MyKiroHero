# 2026-03-09 (Monday)

## Google Calendar MCP — 完成
- 接續 3/7 的設定，OAuth 授權完成
- 需要先跑 `node build/auth-server.js` 完成 OAuth（MCP server 本身不會觸發瀏覽器授權）
- 還需要在 GCP Console 啟用 Calendar API（project 825173270310）
- 全部完成後 list-events 測試成功
- Google 帳號：joe.kao2006@gmail.com
- iPhone 行事曆已同步到 Google Calendar（游泳、籃球都有出現）

## Gmail MCP — 設定完成
- 使用 `@shinzolabs/gmail-mcp@1.7.4`（npx 方式執行）
- OAuth keys 複製到 `~/.gmail-mcp/gcp-oauth.keys.json`
- 跑 `npx -y @shinzolabs/gmail-mcp auth` 完成 OAuth
- credentials 存在 `~/.gmail-mcp/credentials.json`
- 加入 user-level MCP config
- 注意：Gmail auth server 用 port 3000，跟 Gateway 衝突，auth 前要先 `pm2 stop gateway`
- 需要在 GCP Console 啟用 Gmail API（project 825173270310）
- 等待 Joe 重啟 Kiro MCP 後測試寄信

## Gmail MCP — 測試成功 ✅
- Kiro 重啟後 Gmail MCP 連線成功
- `get_profile` 確認帳號：joe.kao2006@gmail.com
- 成功寄出籃球活動通知給 norl_wu@jabil.com
- 主旨：本週四籃球活動通知
- 內容：3/12（四）17:30-18:30
- 中文標題亂碼問題：需用 raw RFC 2822 格式 + RFC 2047 encoded subject 才能正確顯示中文
- autoApprove 已更新為所有 Gmail 工具，不需手動點同意

## 寄送摘要信
- 寄到 joe_liang2208116@gmail.com
- 三月行事曆 3 個活動 + Jira 近一週更新 30 筆
- Jira 帳號 "joe liang FW" 搜尋 assigned issues 為空，可能需確認帳號對應
- 使用 raw RFC 2822 格式避免中文亂碼

## 教訓
- Gmail MCP send_message 的 subject 不支援中文（會亂碼），必須用 raw 參數 + RFC 2047 編碼
- Jira currentUser() 和 email 搜尋都沒結果，可能是 API token 權限或帳號對應問題

## 摘要信寄送完成 ✅
- 已寄到正確地址：joe_liang2208116@jabil.com（之前誤寫 gmail.com，已修正）
- 使用 raw RFC 2822 格式，中文標題正常顯示
- 已透過 WhatsApp 通知 Joe 大神

## Jira 連結規則記錄
- Joe 大神要求：每次讀取 Jira task 都要附上連結
- 已更新 MEMORY.md，加入 Jira section（Base URL、帳號、連結規則）
- 連結格式：`https://jabilcs.atlassian.net/browse/ISSUE-KEY`

## 摘要信 v2（含 Jira 連結）✅
- 重新寄送到 joe_liang2208116@jabil.com
- 每個 Jira issue 都附上 🔗 連結
- Gmail MCP 的 raw 參數透過 tool interface 傳不過去（payload 太大），改用 Node.js 直接呼叫 Gmail API
- 教訓：大型 raw email payload 用 scripts/send-raw-email.js 直接打 Gmail API 更可靠

## HLD 頁面部署到 GitHub Pages ✅
- 建立 `docs/index.html` — MyKiroHero 的 High-Level Design 頁面
- 內容：Overview、系統架構圖（Mermaid）、訊息流程圖、六大元件說明、分層架構、Tech Stack、MCP Tools、API Endpoints、檔案結構
- 深色主題，Mermaid.js CDN 渲染圖表
- 推送到 `joe` remote（JoeLiang2022/MyKiroHero）的 main branch + gh-pages branch
- GitHub Pages 已上線：https://joeliang2022.github.io/MyKiroHero/
